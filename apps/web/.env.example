#
# PulseNote web app runtime configuration
#
# Copy this file to `.env.local` for local development.
# Vercel preview and production deployments must also define this variable.
#
NEXT_PUBLIC_API_BASE_URL=https://api.pulsenotes.xyz
